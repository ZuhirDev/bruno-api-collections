meta {
  name: Disable2FA
  type: http
  seq: 12
}

post {
  url: http://{{base_url}}/api/2fa/disable
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
