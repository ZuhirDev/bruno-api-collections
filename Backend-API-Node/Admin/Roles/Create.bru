meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: http://{{base_url}}/api/roles/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "manage_app",
    "description": "Rol encargado de la gestión completa de roles y permisos en el sistema."
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
